{# <twig:c975LPayment:Basket:DeleteLink quantity="{{ basket.quantity }}"/> #}
{% if quantity > 0 %}
    <button type="button" class="delete-link" data-action="click->basket#delete">{{ 'label.empty_basket'|trans({}, 'payment') }}</button>
{% endif %}