{# <twig:c975LPayment:Basket:Empty/> - said by the page, by the table and by the template the controller swaps in once the last article is taken out, so the three read the same #}
<p class="text text-center lead">{{ 'label.basket_empty'|trans({}, 'payment') }}</p>
