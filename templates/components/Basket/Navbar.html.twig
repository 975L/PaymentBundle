{# <twig:c975LPayment:Basket:Navbar/> #}
{# The bar this bundle's basket controller shows as soon as the basket holds something (see assets/js/basket.js) - it mounts the controller itself, being placed once in the site layout outside every element a shop, a campaign or a print offer carries, so its count and its total, Stimulus targets both, belong to an instance and a page holding no shop still raises it. It lives here and not in a satellite bundle because every bundle filling the basket needs it, whether the site sells products, counterparts or prints #}
<div id="basket-navbar" class="basket-navbar d-none" data-controller="basket">
    <twig:c975LPayment:Basket:ViewButton type="secondary" inline="true"/>
</div>
