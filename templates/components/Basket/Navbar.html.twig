{# <twig:c975LPayment:Basket:Navbar/> #}
{# The bar this bundle's basket controller shows as soon as the basket holds something (see assets/js/basket.js) - the button carries the count and the total it keeps up to date, so nothing is repeated around it. It lives here and not in a satellite bundle because every bundle filling the basket needs it, whether the site sells products, counterparts or prints #}
<div id="basket-navbar" class="basket-navbar d-none">
    <twig:c975LPayment:Basket:ViewButton type="secondary" inline="true"/>
</div>
