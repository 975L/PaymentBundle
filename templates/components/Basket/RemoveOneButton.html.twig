{# <twig:c975LPayment:Basket:RemoveOneButton item="{{ item }}" [type="primary"]/> #}
{% set type = type | default('primary') %}
<button
    data-action="click->basket#removeItem"
    data-type="{{ type }}"
    data-quantity="-1"
    data-item-id="{{ item.item.id }}"
    data-title="{{ item.parent.title ~ ' (' ~ item.item.title ~')' }}"
    data-alert="warning"
    data-text="{{ 'label.removed'|trans({}, 'payment') }}"
    class="btn btn-{{ type }} btn-small lead">
    <twig:c975LUi:Image:Icon src="bundles/c975lpayment/icons/minus.svg" width="16" height="16" class="white"/>
</button>