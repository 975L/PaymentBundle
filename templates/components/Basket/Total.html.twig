{# <twig:c975LPayment:Basket:Total item="{{ item }}" total="{{ total }}" type="{{ type }}"/> #}
<strong><span data-basket-target="itemTotal" data-item-id="{{ type }}-{{ item.id }}">{{ total == 0 ? 'label.free'|trans({}, 'payment') : (total / 100)|format_currency(item.currency) }}</span></strong>
