{# Inviting whoever ordered as a guest to open an account, so their orders follow them - the sign-in itself happens on the order's page, never from an email #}
<twig:c975LPayment:Basket:AccountInvitation basket="{{ basket }}" :link="true"/>
